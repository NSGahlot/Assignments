var n = 5;
 for(var i=1; i<=n; i++)
 {
    var str = "";
    for(var j=1; j<=n; j++)
    {
        if(i==1 || j==1 || i==n || j==n)
        {
            str += "*  ";
        }
        else{
            str += "   ";
        }
    }
    console.log(str);
 }
// Print the 2nd half of the array[Even Array]

var names = ["A", "B","c", "D","E","F", "G", "H"];
var start = names.length/2;
for(var i=start; i<names.length; i++)
{
    console.log(names[i]);
}

// (Even) array formula = numbers.length/2;
// If my array is (Even) we will apply this rule.
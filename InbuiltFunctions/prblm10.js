// the lastIndexof() method returns the last index at whhich given element can be found in the array,
// or (-1) if it is not present. The array is searched backwards, starting at fromIndex.

var elements = ["Fire", "Water", "Air"];
console.log(elements.length);
var element = elements.join(" - ");
console.log(element);